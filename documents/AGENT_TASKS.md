# task1:
Cần cập nhật SRS như hướng dẫn.
Xem xét cập nhatj Project_structure.md cho dđúng với bản hiện tại.

# task2:
Cần kiểm tra và hiển thị UI cảnh báo hồ sơ chưa hoàn thiện theo đúng SRS mới, việc Caching có thể tham khảo: fetch khi mới đăng nhập -> cache, refetch khi về lại home từ edit profile (hoặc lúc edit profile luôn)

# task3:
Cần phải hiển thị lỗi một cách dễ hiểu với người dùng (mấy lỗi mạng như HTTP 401, 503, 500 thì phải đổi thành ngôn ngữ tự nhiên tương ứng).

# task5:
Kiểm tra lại phần Hello ở main xem có phải mock không tại tôi thấy tên ở đó khác trong profile (kể cả lúc dùng bản remote)
Xem lại luôn các api xem có chỗ nào tự động mock khi remote lỗi không, phải sửa lại vì remote là bản production khi chuyển qua mode đó thì không được phép mock nữa.

